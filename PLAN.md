TOP画面はChatGPTみたいなUIにする。
他のAIサービスとの差別化はMCPサーバーをホスティングすることができること。
MCPサーバーのホスティングは基本的に無料で、AI利用は有料にする。
MCPサーバー自体は別のAIサービスから利用することもできる。
MCPサーバーを検索できるようなMCPサーバーも作る。
MCPサーバーはworkersみたいな感じで動くことを想定。
プログラミング言語とまではいかなくてもスクリプトみたいにこのサービスが提供するapiを利用するための定義ファイルを用意する。
GUIでスクリプト操作できるようにする。apiを利用して予約の管理とかそういうMCPの商用利用を想定。
MCPサーバーもある一定以上の負荷がかかると有料にする。
stripeで決済を行う。
AIはLangGlaphを利用してmanusみたいな万能エージェントにする。

サーバーサイドはCloudflare Workers + Hono
DBはPostgreSQL + Prisma
フロントエンドはVite + SolidJS